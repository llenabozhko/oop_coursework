<local:BaseWindow x:Class="oop_coursework.Views.TeacherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:oop_coursework.Views"
        Title="Teacher Dashboard" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="*"/> <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Border Grid.Row="1"
                Background="#F5F5F5"
                Padding="30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Welcome -->
                    <RowDefinition Height="Auto"/> <!-- Subject Info -->
                    <RowDefinition Height="Auto"/> <!-- Settings -->
                    <RowDefinition Height="*"/> <!-- Students Grid -->
                    <RowDefinition Height="Auto"/> <!-- Save Button -->
                </Grid.RowDefinitions>

                <!-- Welcome Section -->
                <TextBlock Grid.Row="0"
                          x:Name="WelcomeText"
                          FontSize="24"
                          FontWeight="SemiBold"
                          Margin="0,0,0,20"/>

                <!-- Subject Info Section -->
                <Border Grid.Row="1"
                        Background="White"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,20"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Your Subject"
                                 FontWeight="SemiBold"
                                 FontSize="18"/>
                        <TextBlock x:Name="SubjectNameText"
                                 FontSize="16"
                                 Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Settings Section -->
                <Border Grid.Row="2"
                        Background="White"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,20"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Exam Date Settings -->
                        <StackPanel Grid.Column="0"
                                  Margin="0,0,40,0">
                            <TextBlock Text="Exam Date"
                                     FontWeight="SemiBold"
                                     Margin="0,0,0,8"/>
                            <DatePicker x:Name="ExamDatePicker"
                                      Width="200"/>
                        </StackPanel>

                        <!-- Assessment Type Settings -->
                        <StackPanel Grid.Column="1"
                                  Margin="0,0,40,0">
                            <TextBlock Text="Assessment Type"
                                     FontWeight="SemiBold"
                                     Margin="0,0,0,8"/>
                            <ComboBox x:Name="AssessmentTypeComboBox"
                                    Width="200">
                                <ComboBoxItem Content="Exam"/>
                                <ComboBoxItem Content="Test"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Update Button -->
                        <Button Grid.Column="2"
                                Content="Update Settings"
                                Click="UpdateSettings_Click"
                                Height="30"
                                Width="120"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <!-- Students Grid -->
                <Border Grid.Row="3"
                        Background="White"
                        CornerRadius="8"
                        Padding="25"
                        Margin="0,0,0,20"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Student Grades"
                                 FontWeight="SemiBold"
                                 FontSize="18"
                                 Margin="0,0,0,20"/>

                        <ScrollViewer Grid.Row="1"
                                    VerticalScrollBarVisibility="Auto">
                            <DataGrid x:Name="StudentsGrid"
                                    AutoGenerateColumns="False"
                                    IsReadOnly="False"
                                    HeadersVisibility="Column"
                                    GridLinesVisibility="Horizontal"
                                    BorderThickness="1"
                                    BorderBrush="#E0E0E0"
                                    RowHeaderWidth="0"
                                    VerticalGridLinesBrush="Transparent"
                                    HorizontalGridLinesBrush="#E0E0E0"
                                    Background="Transparent"
                                    RowBackground="Transparent"
                                    AlternatingRowBackground="#F8F8F8"
                                    VerticalScrollBarVisibility="Visible">
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Padding" Value="10,5"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Padding" Value="10,5"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="DataGridCell">
                                                    <Border Padding="{TemplateBinding Padding}"
                                                            BorderThickness="0"
                                                            Background="{TemplateBinding Background}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                            </Trigger>
                                            <Trigger Property="IsEditing" Value="True">
                                                <Setter Property="Background" Value="White"/>
                                                <Setter Property="Foreground" Value="Black"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <Trigger Property="IsKeyboardFocused" Value="True">
                                                <Setter Property="Foreground" Value="Black"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.Resources>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Student Name"
                                                      Binding="{Binding Student.FullName}"
                                                      Width="2*"
                                                      IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Specialty"
                                                      Binding="{Binding Student.Specialty}"
                                                      Width="2*"
                                                      IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Score"
                                                      Binding="{Binding Grade.Score}"
                                                      Width="*"/>
                                </DataGrid.Columns>
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Grade.Subject.IsExamSoon}" Value="True">
                                                <Setter Property="Background" Value="#FFECB3"/>
                                                <Setter Property="ToolTip" Value="Exam is coming up in 10 days or less!"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Style.Resources>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#E3F2FD"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#E3F2FD"/>
                                        </Style.Resources>
                                    </Style>
                                </DataGrid.RowStyle>
                            </DataGrid>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Save Button -->
                <Button Grid.Row="4"
                        Content="Save Grades"
                        Click="SaveGrades_Click"
                        Height="40"
                        Width="150"
                        HorizontalAlignment="Right"
                        Background="#2196F3"
                        Foreground="White"
                        FontWeight="SemiBold"/>
            </Grid>
        </Border>
    </Grid>
</local:BaseWindow>
